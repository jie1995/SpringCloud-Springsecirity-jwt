spring:
  application:
    name: cloud-hystrix-dashboard
server:
  port: 2001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /